{
  "license": "MIT",
  "scripts": {
    "build": "browserify src/main.ts -p [tsify] > dist/main.js && web-ext build --overwrite-dest",
    "fix": "prettier --write **/*.ts --list-different && tslint --fix --project .",
    "lint": "prettier **/*.ts --list-different && tslint --project ."
  },
  "devDependencies": {
    "browserify": "^16.2.3",
    "prettier": "^1.15.3",
    "tsify": "^4.0.1",
    "tslint": "^5.12.0",
    "tslint-config-prettier": "^1.17.0",
    "typescript": "^3.2.2"
  },
  "dependencies": {
    "moment": "^2.23.0"
  }
}
