name: lint
on: push
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          cache: "yarn"
          node-version: "24.x"
      - run: yarn --frozen-lockfile
      - run: yarn package
      - run: yarn lint
